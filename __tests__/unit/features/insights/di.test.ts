// insights/di.ts is just a placeholder comment
// Test that the module can be imported
describe('insights/di', () => {
	it('should export insights di module', () => {
		// Just verify the module can be imported
		const insightsDi = require('@/features/insights/di');
		expect(insightsDi).toBeDefined();
	});
});
